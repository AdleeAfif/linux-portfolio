/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
  assetPrefix: "./",
  basePath: "/linux-portfolio",
};

module.exports = nextConfig;
